#include "_putchar.c"

/**
 * main - Print `_putchar`
 *
 * Return: Always 0.
 */
int main(void)
{
	int out[] = "_putchar";

	for(int i = 0; i < sizeof(out); i++)
	{
		_putchar(out[i]);
	}
	_putchar('\n');
	return (0);
}
